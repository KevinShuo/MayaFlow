# -*- coding: utf-8 -*-
import os
import tempfile


def get_temp_path():
    t_p = tempfile.gettempdir()
    p = os.path.join(t_p, 'm_deadline_cache')
    if not os.path.exists(p):
        os.makedirs(p)
    return p
